name: Repository labeler

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic labeler is disabled until a repository-specific label policy is defined."
