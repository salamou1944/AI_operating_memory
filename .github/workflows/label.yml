name: Retired labeler template

# The repository does not currently maintain a labeler configuration.
# This workflow is kept manual-only to preserve the artifact without failing
# every pull request because .github/labeler.yml is intentionally absent.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic labeler is retired until a repository-specific label policy is defined."
