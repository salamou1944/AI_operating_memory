name: Retired workflow - nextjs.yml

# Retired repository-template workflow. It is intentionally manual-only and
# performs no external deployment because this repository has no configured
# target for this template.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "nextjs.yml is retired; no deployment target is configured."
