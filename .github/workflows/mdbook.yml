name: Build mdBook documentation (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "mdBook workflow retired; no mdBook source is configured in this repository."
